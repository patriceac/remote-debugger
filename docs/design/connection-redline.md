# Connection screen and wake settings

Approved on 2026-09-23. References: [connection](connection-redline.png), [wake modal](wake-modal-redline.png).

The computer list, selected-computer inspector, restrained sidebar and teal accents follow the approved images. Connection settings can collapse and the inspector scrolls independently on smaller windows. There is no Show details feature. Keep transfer measurements separate from estimated timing; only observed stages can be marked complete.

Wake settings is a modal with MAC address, sender, optional broadcast/router address and UDP port. Save stores configuration; Wake computer remains a separate action. Existing validation and local/helper routing remain authoritative.

Both references were generated with the built-in imagegen tool. Final prompt direction: preserve the native desktop composition, remove the redundant details disclosure, retain expanded WAN/wake controls, and show the four-field wake modal over an idle offline-computer view. Example devices and progress in these images are illustrative.

The ETA value no longer has the approximation prefix shown in the historical connection reference; this was explicitly removed on 2026-09-23.
